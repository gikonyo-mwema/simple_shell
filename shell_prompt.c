#include "shell.h"
/**
 * print_prompt - outputs the user prompt
 * @prompt: dollar sign field
 * Return: dollarsign + user entry field
 */
void print_prompt(const char *prompt)
{
	_print_shell(prompt);
}
